## ADDED Requirements
### Requirement: Language Config XML Update
The `Language/Config.xml` file SHALL be updated to include an entry for the Vietnamese language (`Vi`).

#### Scenario: 'Vi' entry added
- **WHEN** the change is applied
- **THEN** `Language/Config.xml` contains a new entry enabling the 'Vi' language.
