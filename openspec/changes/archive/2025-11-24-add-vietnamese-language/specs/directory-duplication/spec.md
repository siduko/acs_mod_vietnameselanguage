## ADDED Requirements
### Requirement: Language Directory Duplication
The `Language/Th` directory and its entire contents SHALL be duplicated to a new directory named `Language/Vi`.

#### Scenario: Successful duplication
- **WHEN** the change is applied
- **THEN** a new directory `Language/Vi` exists with identical content to `Language/Th`.
