## MODIFIED Requirements
### Requirement: Language Directory Duplication
The content within the `Language/Vi` directory SHALL be updated with actual Vietnamese translations for its files, replacing the placeholder Thai content.

#### Scenario: `Language/Vi` content updated with translations
- **WHEN** the translation change is applied
- **THEN** all translatable files within `Language/Vi` contain Vietnamese translations.
