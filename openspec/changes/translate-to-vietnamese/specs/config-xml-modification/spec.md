## MODIFIED Requirements
### Requirement: Language Config XML Update
The `Language/Config.xml` file SHALL be updated with actual Vietnamese translations for relevant entries and ensure the 'Vi' language is correctly configured.

#### Scenario: 'Vi' entry updated with translations
- **WHEN** the translation change is applied
- **THEN** `Language/Config.xml` contains updated entries with Vietnamese translations and the 'Vi' language is correctly configured.
